import Link from 'next/link';
import type { Video } from '../page';
const fallback = ['https://images.unsplash.com/photo-1549490349-8643362247b5?auto=format&fit=crop&w=600&q=80', 'https://images.unsplash.com/photo-1500534314209-a25ddb2bd429?auto=format&fit=crop&w=600&q=80', 'https://images.unsplash.com/photo-1518391846015-55a9cc003b25?auto=format&fit=crop&w=600&q=80'];
export function CatalogueRow({ title, videos }: { title: string; videos: Video[] }) { return <section className="section"><div className="sectionTitle"><h2>{title}</h2><span>View all</span></div>{videos.length ? <div className="row">{videos.map((video, index) => <Link className="poster" href={`/watch/${video.id}`} key={video.id}><div className="posterImage"><img src={video.thumbnailKey ?? fallback[index % fallback.length]} alt="" /></div><span className="posterTitle">{video.title}</span><span className="muted">{video.durationSeconds ? `${Math.round(video.durationSeconds / 60)} min` : 'Feature'} </span></Link>)}</div> : <div className="empty">The catalogue will appear here once videos are published.</div>}</section>; }
