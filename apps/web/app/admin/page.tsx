'use client';
import Link from 'next/link';
import { useRouter } from 'next/navigation';
import { useState } from 'react';

export default function AdminHome() { const router = useRouter(); const [tmdbId, setTmdbId] = useState(''); const [season, setSeason] = useState(''); const [episode, setEpisode] = useState(''); const open = (event: React.FormEvent) => { event.preventDefault(); if (!/^\d+$/.test(tmdbId)) return; router.push(season && episode ? `/admin/${tmdbId}/${season}/${episode}` : `/admin/${tmdbId}`); }; return <main className="adminShell"><header className="adminHeader"><Link className="brand" href="/">Northstar</Link><Link className="muted" href="/admin/login">Account</Link></header><section className="authPanel"><span className="eyebrow">Administration</span><h1>Open media</h1><form className="authForm" onSubmit={open}><label>TMDb ID<input required inputMode="numeric" value={tmdbId} onChange={(event) => setTmdbId(event.target.value)} placeholder="Movie or series ID" /></label><label>Season number <span className="muted">Optional for movies</span><input inputMode="numeric" value={season} onChange={(event) => setSeason(event.target.value)} placeholder="1" /></label><label>Episode number <span className="muted">Optional for movies</span><input inputMode="numeric" value={episode} onChange={(event) => setEpisode(event.target.value)} placeholder="1" /></label><button className="button">Open editor</button></form></section></main>; }
